FROM node:22-alpine

WORKDIR /app

# Copy workspace root package.json for workspaces resolution
COPY package.json ./
COPY packages/shared ./packages/shared
COPY apps/web ./apps/web

RUN npm install
RUN cd packages/shared && npm run build

WORKDIR /app/apps/web

EXPOSE 3000
CMD ["npm", "run", "dev"]
